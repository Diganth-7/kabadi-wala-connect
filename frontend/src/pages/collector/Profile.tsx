import { LogOut, UserCircle, Languages, Shield } from 'lucide-react'
import { useNavigate } from 'react-router-dom'
import { AppLayout } from '../../layouts/AppLayout'
import { PageHeader } from '../../components/PageHeader'
import { useApp } from '../../contexts/AppContext'
export default function Profile(){const {user,logout,setRole}=useApp();const nav=useNavigate();return <AppLayout><div className="mx-auto max-w-xl"><PageHeader title="Profile"/><div className="card p-6"><div className="flex items-center gap-4"><div className="flex h-16 w-16 items-center justify-center rounded-full bg-mint text-leaf"><UserCircle size={40}/></div><div><div className="text-xl font-black">{user?.name}</div><div className="text-sm text-gray-500">{user?.phone} · {user?.area}</div></div></div><div className="mt-6 space-y-2"><button className="ghost w-full justify-start"><Languages/> Language</button><button className="ghost w-full justify-start"><Shield/> Frontend validation only</button><button className="ghost w-full justify-start" onClick={()=>{setRole('RECYCLER');nav('/recycler')}}>Switch to Recycler demo</button><button className="ghost w-full justify-start" onClick={()=>{setRole('ADMIN');nav('/admin')}}>Switch to Admin demo</button><button className="danger w-full justify-start" onClick={()=>{logout();nav('/login')}}><LogOut/> Log out</button></div></div></div></AppLayout>}
