import { CheckCircle2, ArrowRight, Copy } from 'lucide-react'
import { Link, useParams } from 'react-router-dom'
import { AppLayout } from '../../layouts/AppLayout'
export default function LotCreated(){const {id}=useParams();return <AppLayout><div className="mx-auto max-w-xl text-center"><div className="card p-7"><div className="mx-auto flex h-20 w-20 items-center justify-center rounded-full bg-mint text-leaf"><CheckCircle2 size={48}/></div><h1 className="mt-5 text-3xl font-black">Lot created</h1><p className="mt-2 text-gray-500">Your scrap is ready for price discovery and recycler matching.</p><div className="mt-6 rounded-2xl bg-ink p-5 text-left text-white"><div className="text-xs uppercase tracking-wider text-white/60">Lot ID</div><div className="mt-1 flex items-center justify-between text-2xl font-black"><span>{id}</span><Copy size={20}/></div></div><Link to={`/collector/prices?lot=${id}`} className="primary mt-5 w-full">View price <ArrowRight/></Link><Link to={`/collector/recyclers?lot=${id}`} className="secondary mt-3 w-full">Find recycler</Link></div></div></AppLayout>}
