import { useEffect,useState } from 'react'
import { Package, Truck, IndianRupee, CheckCircle2 } from 'lucide-react'
import { AppLayout } from '../../layouts/AppLayout'
import { MetricCard } from '../../components/MetricCard'
import { pickupService } from '../../services'
import type { Pickup } from '../../types'
export default function RecyclerDashboard(){const [pickups,setPickups]=useState<Pickup[]>([]);useEffect(()=>{Promise.all([]).then(()=>setPickups([]))},[]);return <AppLayout><div className="mx-auto max-w-5xl"><div className="mb-6"><p className="text-sm text-gray-500">Recycler workspace</p><h1 className="text-3xl font-black">Green Recycling Pvt Ltd</h1><span className="mt-2 inline-flex items-center gap-1 rounded-full bg-mint px-3 py-1 text-xs font-bold text-leaf"><CheckCircle2 size={14}/> Authorized recycler</span></div><div className="grid grid-cols-2 gap-3 md:grid-cols-4"><MetricCard label="Incoming requests" value="6" icon={<Package/>}/><MetricCard label="Pickups today" value="4" icon={<Truck/>}/><MetricCard label="This month" value="₹48,620" icon={<IndianRupee/>}/><MetricCard label="Pending handovers" value="2" icon={<CheckCircle2/>}/></div><div className="card mt-5 p-6"><h2 className="text-xl font-black">Quick actions</h2><div className="mt-4 grid gap-3 md:grid-cols-2"><Action to="/recycler/requests" title="Incoming requests" text="Accept or reject collector requests"/><Action to="/recycler/pickups" title="Pickup management" text="Advance pickup status"/><Action to="/recycler/materials" title="Materials & prices" text="Set prices and accepted materials"/><Action to="/recycler/transactions" title="Transactions" text="Review completed handovers"/></div></div></div></AppLayout>}
function Action({to,title,text}:{to:string;title:string;text:string}){return <a href={to} className="rounded-2xl border border-black/5 bg-mint p-5 hover:shadow-sm"><div className="font-black">{title}</div><div className="mt-1 text-sm text-gray-500">{text}</div></a>}
